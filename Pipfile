[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
ruff = "==0.4.3"
ruff-lsp = "==0.0.53"
pre-commit = "==3.7.0"

[packages]
pip = "24.0"

[requires]
python_version = "3.11"


[scripts]
test = "pytest"
pre-commit = "pre-commit run --show-diff-on-failure --color=always --all-files"